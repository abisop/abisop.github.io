import{_ as t,c as o,al as n,o as a}from"./chunks/framework.NFAqBSgQ.js";const h=JSON.parse('{"title":"ftpc FTP Client","description":"","frontmatter":{},"headers":[],"relativePath":"en/applications/examples/ftpc/index.md","filePath":"en/applications/examples/ftpc/index.md"}'),i={name:"en/applications/examples/ftpc/index.md"};function s(c,e,r,d,l,p){return a(),o("div",null,e[0]||(e[0]=[n(`<h1 id="ftpc-ftp-client" tabindex="-1"><code>ftpc</code> FTP Client <a class="header-anchor" href="#ftpc-ftp-client" aria-label="Permalink to &quot;\`ftpc\` FTP Client&quot;">​</a></h1><p>This is a simple FTP client shell used to exercise the capabilities of the FTPC library (<code>apps/netutils/ftpc</code>).</p><p>From NSH, the startup command sequence is as follows. This is only an example, your configuration could have different mass storage devices, mount paths, and FTP directories:</p><pre><code>nsh&gt; mount -t vfat /dev/mmcsd0 /tmp  # Mount the SD card at /tmp
nsh&gt; cd /tmp                         # cd into the /tmp directory
nsh&gt; ftpc &lt;host&gt; &lt;port&gt;              # Start the FTP client
nfc&gt; login &lt;name&gt; &lt;password&gt;         # Log into the FTP server
nfc&gt; help                            # See a list of FTP commands
</code></pre><p>where <code>&lt;host&gt;</code> is the IP address or hostname of the FTP server and <code>&lt;port&gt;</code> is an optional port number.</p><p><strong>Note</strong>: By default, FTPC uses <code>readline</code> to get data from <code>stdin</code>. So your defconfig file must have the following build path:</p><pre><code>CONFIG_SYSTEM_READLINE=y
</code></pre><p><strong>Note</strong>: If you use the ftpc task over a telnet NSH connection, then you should set the following configuration item:</p><pre><code>CONFIG_EXAMPLES_FTPC_FGETS=y
</code></pre><p>By default, the FTPC client will use <code>readline()</code> to get characters from the console. Readline includes and command-line editor and echos characters received in stdin back through <code>stdout</code>. Neither of these behaviors are desire-able if Telnet is used.</p><p>You may also want to define the following in your configuration file. Otherwise, you will have not feedback about what is going on:</p><pre><code>CONFIG_DEBUG_FEATURES=y
CONFIG_DEBUG_INFO=y
CONFIG_DEBUG_FTPC=y
</code></pre>`,12)]))}const m=t(i,[["render",s]]);export{h as __pageData,m as default};
