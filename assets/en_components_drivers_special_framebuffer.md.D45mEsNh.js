import{_ as i,c as a,al as t,o as r}from"./chunks/framework.NFAqBSgQ.js";const h=JSON.parse('{"title":"Frame Buffer Drivers","description":"","frontmatter":{},"headers":[],"relativePath":"en/components/drivers/special/framebuffer.md","filePath":"en/components/drivers/special/framebuffer.md"}'),o={name:"en/components/drivers/special/framebuffer.md"};function s(d,e,l,c,n,f){return r(),a("div",null,e[0]||(e[0]=[t(`<h1 id="frame-buffer-drivers" tabindex="-1">Frame Buffer Drivers <a class="header-anchor" href="#frame-buffer-drivers" aria-label="Permalink to &quot;Frame Buffer Drivers&quot;">​</a></h1><p>A framebuffer is a memory-mapped buffer that represents all the pixels necessary to drive a video display.</p><p>The Frame Buffer driver is intended to be used in the following scenarios:</p><ol><li>Whenever it is necessary to hold all the pixels that would be used to drive a video display. This includes: <ol><li>Graphics libraries that directly access the underlying framebuffer;</li><li>Advanced UIs (e.g. alpha blending) that need to read back the image data;</li></ol></li><li>Applications that expect the framebuffer to exist;</li></ol><h2 id="binding" tabindex="-1">Binding <a class="header-anchor" href="#binding" aria-label="Permalink to &quot;Binding&quot;">​</a></h2><p>LCD and frame buffer drivers usually are not directly accessed by user code, but are usually bound to another, higher-level device driver. In general, the binding sequence is:</p><ol><li>Get an instance of <code>struct fb_vtable_s</code> from the hardware-specific frame buffer device driver, and</li><li>Provide that instance to the initialization method of the higher-level device driver.</li></ol><h3 id="generic-lcd-frame-buffer" tabindex="-1">Generic LCD Frame Buffer <a class="header-anchor" href="#generic-lcd-frame-buffer" aria-label="Permalink to &quot;Generic LCD Frame Buffer&quot;">​</a></h3><p>This example will walk through the path from userspace to hardware-specific details on how an LCD screen is bound to a framebuffer.</p><ol><li><p><code>include/nuttx/video/fb.h</code> provides all structures and APIs needed to work with frame buffer drivers:</p><ol><li><code>drivers/video/fb.c</code> is the higher-level device driver. An instance of <code>struct fb_vtable_s</code> will be provided to it;</li><li><code>fb_register</code> registers the framebuffer character device at <code>/dev/fbN</code> where N is the display number;</li><li>It also provides the prototype of <code>up_fbinitialize</code>, which may be defined by: <ol><li>An specific device into <code>arch/&lt;arch&gt;/src/&lt;chip&gt;</code> directory;</li><li>By the LCD framebuffer adapter in <code>drivers/lcd/lcd_framebuffer.c</code>, which provides an intermediary interface between the Frame Buffer Driver and the LCD screen drivers;</li></ol></li></ol></li><li><p>Let&#39;s consider we are using the LCD framebuffer (<code>CONFIG_LCD_FRAMEBUFFER = y</code>):</p><ol><li>This interface implements the <code>up_fbinitialize</code> which: <ol><li>Provides the instance of <code>struct fb_vtable_s</code> (a member of <code>struct lcdfb_dev_s</code>);</li><li>Calls <code>board_lcd_initialize</code> and <code>board_lcd_getdev</code> LCD-specific functions. These functions are defined in <code>boards/&lt;arch&gt;/&lt;chip&gt;/&lt;board&gt;/src</code> and prototyped in <code>include/nuttx/board.h</code>;</li></ol></li></ol></li><li><p>Finally, the LCD screen drivers are usually available at <code>drivers/lcd/</code> and implement the callbacks defined at <code>include/nuttx/lcd/lcd.h</code>:</p><blockquote><ol><li><code>include/nuttx/lcd/lcd.h</code> provides structures and APIs needed to work with LCD screens, whereas using the framebuffer adapter or the [[lcd]{.title-ref};]([lcd]{.title-ref};.md)</li></ol></blockquote></li></ol><h2 id="vsync" tabindex="-1">VSYNC <a class="header-anchor" href="#vsync" aria-label="Permalink to &quot;VSYNC&quot;">​</a></h2><p>Vertical synchronization (VSync) synchronizes the frame rate of an application&#39;s graphics with the refresh rate of the display, helping to establish stability. If not synchronized, it may cause screen tearing, which is the effect of the image appearing to have horizontal jagged edges or ghosting across the entire screen.</p><h3 id="vsync-offset" tabindex="-1">VSYNC Offset <a class="header-anchor" href="#vsync-offset" aria-label="Permalink to &quot;VSYNC Offset&quot;">​</a></h3><p>During the VSYNC event, the screen starts displaying frame N while the renderer begins compositing window for frame N+1. The application processes waiting input and generates frame N+2. When the renderer has a short rendering time, it can cause a delay of almost two frames from the end of rendering to the completion of screen display. To solve this problem, <code>FBIOSET_VSYNCOFFSET</code> can be used to set the VSYNC offset time (in microseconds) and reduce the delay from input device to screen using the VSYNC offset.</p><h2 id="examples" tabindex="-1">Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;Examples&quot;">​</a></h2><p>Examples apply to specific cases of the <code>genericlcdfb</code>{.interpreted-text role=&quot;ref&quot;}:</p><h3 id="ttgo-t-display-esp32-board" tabindex="-1">TTGO T-Display ESP32 board <a class="header-anchor" href="#ttgo-t-display-esp32-board" aria-label="Permalink to &quot;TTGO T-Display ESP32 board&quot;">​</a></h3><p>This board contains an ST7789 TFT Display (135x240). By selecting the <code>ttgo_t_display_esp32:lvgl_fb</code> config, the <code>lvgldemo</code> example will be built with the framebuffer interface.</p><ul><li><code>boards/xtensa/esp32/ttgo_t_display_esp32/src/esp32_bringup.c</code> registers the framebuffer driver:</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#ifdef CONFIG_VIDEO_FB</span></span>
<span class="line"><span>  ret = fb_register(0, 0);</span></span>
<span class="line"><span>  if (ret &lt; 0)</span></span>
<span class="line"><span>    {</span></span>
<span class="line"><span>      syslog(LOG_ERR, &quot;ERROR: Failed to initialize Frame Buffer Driver.\\n&quot;);</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span>#endif</span></span></code></pre></div><ul><li><p><code>up_fbinitialize</code> from the frame buffer adapter will then be called as <code>CONFIG_LCD_FRAMEBUFFER = y</code>:</p><blockquote><ul><li><p><code>board_lcd_initialize</code> and <code>board_lcd_getdev</code> are defined at <code>boards/xtensa/esp32/common/src/esp32_st7789.c</code>:</p><blockquote><ul><li><code>board_lcd_initialize</code> initializes the LCD hardware on the board by defining the SPI interface which is connected to the display controller;</li><li><code>board_lcd_getdev</code> calls the <code>st7789_lcdinitialize</code> and returns a reference to the LCD object for the specified LCD;</li><li><code>st7789_lcdinitialize</code> is part of the LCD screen driver at <code>drivers/lcd/st7789.c</code>;</li></ul></blockquote></li></ul></blockquote></li><li><p>The LVGL demo application (<code>lvgldemo</code>) makes use of the <code>ioctl</code> system call to trigger a <code>FBIO_UPDATE</code> request to the higher-level device driver to refresh the LCD screen with framebuffer data:</p></li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ioctl(state.fd, FBIO_UPDATE, (unsigned long)((uintptr_t)&amp;fb_area));</span></span></code></pre></div><h3 id="nuttx-simulator" tabindex="-1">NuttX Simulator <a class="header-anchor" href="#nuttx-simulator" aria-label="Permalink to &quot;NuttX Simulator&quot;">​</a></h3><p>[[NuttX Simulator &lt;/platform](\`NuttX Simulator &lt;/platform.md)s/sim/sim/index&gt;]{.title-ref} provides a X11-based framebuffer driver to simulate the framebuffer usage into a X11-compatible host.</p><p>By selecting the <code>sim:lvgl_fb</code> config, the <code>lvgldemo</code> example will be built with the framebuffer driver.</p><ul><li><p><code>boards/sim/sim/sim/src/sim_bringup.c</code> registers the framebuffer driver the same way <code>ttgotdisplayesp32_fb</code>{.interpreted-text role=&quot;ref&quot;};</p></li><li><p><code>arch/sim/src/sim/up_framebuffer.c</code> and <code>arch/sim/src/sim/up_x11framebuffer.c</code> will be built as <code>CONFIG_SIM_FRAMEBUFFER = y</code> and <code>CONFIG_SIM_X11FB = y</code> are set, respectively;</p><blockquote><ul><li><code>up_framebuffer.c</code> provides <code>up_fbinitialize</code> and,</li><li>calls <code>up_x11initialize</code> from <code>up_x11framebuffer.c</code> that initializes a X11-based window as a framebuffer. This is the underlying &quot;driver&quot;.</li></ul></blockquote></li><li><p>The LVGL demo application (<code>lvgldemo</code>) makes use of the <code>ioctl</code> system call to trigger a <code>FBIO_UPDATE</code> request to the higher-level device driver in order to refresh the LCD screen with framebuffer data as usual;</p></li></ul><p>Warning</p><p>One must consider that framebuffer requires that the entire display&#39;s pixels to be represented. Considering a 320x480 @RGB565 LCD screen, that would be 300KiB, which it&#39;d be too much for a memory-constrained device.</p><p>However, when memory is not a constraint, framebuffer may offer applications a faster way to update display contents once writing to the RAM-mapped buffer is faster than doing multiple SPI transfers.</p><p>For memory-constrained devices, consider using [[lcd]{.title-ref}.]([lcd]{.title-ref}..md)</p>`,30)]))}const u=i(o,[["render",s]]);export{h as __pageData,u as default};
