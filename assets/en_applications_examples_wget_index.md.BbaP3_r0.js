import{_ as t,c as o,al as i,o as l}from"./chunks/framework.NFAqBSgQ.js";const E=JSON.parse('{"title":"wget Web Client","description":"","frontmatter":{},"headers":[],"relativePath":"en/applications/examples/wget/index.md","filePath":"en/applications/examples/wget/index.md"}'),s={name:"en/applications/examples/wget/index.md"};function a(n,e,d,c,r,p){return l(),o("div",null,e[0]||(e[0]=[i(`<h1 id="wget-web-client" tabindex="-1"><code>wget</code> Web Client <a class="header-anchor" href="#wget-web-client" aria-label="Permalink to &quot;\`wget\` Web Client&quot;">​</a></h1><p>A simple web client example. It will obtain a file from a server using the HTTP protocol. Settings unique to this example include:</p><ul><li><code>CONFIG_EXAMPLES_WGET_URL</code> – The URL of the file to get</li><li><code>CONFIG_EXAMPLES_WGET_NOMAC</code> – (May be defined to use software assigned MAC)</li><li><code>CONFIG_EXAMPLES_WGET_IPADDR</code> – Target IP address</li><li><code>CONFIG_EXAMPLES_WGET_DRIPADDR</code> – Default router IP address</li><li><code>CONFIG_EXAMPLES_WGET_NETMASK</code> – Network mask</li></ul><p>This example uses <code>netutils/webclient</code>. Additional configuration settings apply to that code as follows (but built-in defaults are probably OK):</p><ul><li><code>CONFIG_WEBCLIENT_GETMIMETYPE</code></li><li><code>CONFIG_WEBCLIENT_MAXHTTPLINE</code></li><li><code>CONFIG_WEBCLIENT_MAXMIMESIZE</code></li><li><code>CONFIG_WEBCLIENT_MAXHOSTNAME</code></li><li><code>CONFIG_WEBCLIENT_MAXFILENAME</code></li></ul><p>Of course, the example also requires other settings including <code>CONFIG_NET</code> and <code>CONFIG_NET_TCP</code>. The example also uses the uIP resolver which requires <code>CONFIG_UDP</code>.</p><p><strong>Warning</strong>: As of this writing, <code>wget</code> is untested on the target platform. At present it has been tested only in the host-based configuration described in the following note. The primary difference is that the target version will rely on the also untested uIP name resolver.</p><p><strong>Note</strong>: For test purposes, this example can be built as a host-based <code>wget</code> function. This can be built as follows:</p><pre><code>cd examples/wget
make -f Makefile.host
</code></pre><p>Applications using this example will need to enable the following <code>netutils</code> libraries in the <code>defconfig</code> file: :</p><pre><code>CONFIG_NETUTILS_NETLIB=y
CONFIG_NETDB_DNSCLIENT=y
CONFIG_NETUTILS_WEBCLIENT=y
</code></pre>`,11)]))}const T=t(s,[["render",a]]);export{E as __pageData,T as default};
