import{_ as s,c as t,al as a,o as n}from"./chunks/framework.NFAqBSgQ.js";const h=JSON.parse('{"title":"RPMSG File System","description":"","frontmatter":{},"headers":[],"relativePath":"en/components/filesystem/rpmsgfs.md","filePath":"en/components/filesystem/rpmsgfs.md"}'),o={name:"en/components/filesystem/rpmsgfs.md"};function i(r,e,p,c,l,m){return n(),t("div",null,e[0]||(e[0]=[a(`<h1 id="rpmsg-file-system" tabindex="-1">RPMSG File System <a class="header-anchor" href="#rpmsg-file-system" aria-label="Permalink to &quot;RPMSG File System&quot;">​</a></h1><p>Once RPMsg link is available, we can use rpmsg file system to mount remote directories with the help of RPMsg file system drivers.</p><p>Here we show an example of mounting and using a master file system path from remote side, it is as easy as using a local file system.</p><h2 id="building" tabindex="-1">Building <a class="header-anchor" href="#building" aria-label="Permalink to &quot;Building&quot;">​</a></h2><p>At file system server side (the master), we need enable the <code>CONFIG_FS_RPMSGFS_SERVER</code> configuration.</p><p>At file system client side (the remote), we need enable the <code>CONFIG_FS_RPMSGFS</code> configuration.</p><p>Then we build the two sides accordingly.</p><h2 id="running" tabindex="-1">Running <a class="header-anchor" href="#running" aria-label="Permalink to &quot;Running&quot;">​</a></h2><p>Using the following command to mount the master&#39;s <code>/proc</code> file system to <code>/proc.master</code> from the <code>nsh</code> sessino of the remote node.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>remote&gt; mount -t rpmsgfs -o cpu=master,fs=/proc /proc.master</span></span>
<span class="line"><span>remote&gt; cat /proc/uptime /proc.master/uptime </span></span>
<span class="line"><span>      39.06                                                                      </span></span>
<span class="line"><span>      39.06                                                                      </span></span>
<span class="line"><span>remote&gt;</span></span></code></pre></div><p>Note the <code>-o cpu=master,fs=/proc</code> specifies the <code>master</code> node&#39;s <code>/proc</code> path as the source, the <code>/proc.master</code> is the mount point at remote side. All files under that mount point is actually hosted at the master side. The <code>-t rpmsgfs</code> selects the RPMsg file system driver to serve the operation.</p>`,11)]))}const u=s(o,[["render",i]]);export{h as __pageData,u as default};
