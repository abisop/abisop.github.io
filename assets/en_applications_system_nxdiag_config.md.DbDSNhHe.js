import{_ as e,c as o,al as d,o as i}from"./chunks/framework.NFAqBSgQ.js";const g=JSON.parse('{"title":"Configuration Settings","description":"","frontmatter":{},"headers":[],"relativePath":"en/applications/system/nxdiag/config.md","filePath":"en/applications/system/nxdiag/config.md"}'),n={name:"en/applications/system/nxdiag/config.md"};function a(s,t,c,l,r,p){return i(),o("div",null,t[0]||(t[0]=[d('<h1 id="configuration-settings" tabindex="-1">Configuration Settings <a class="header-anchor" href="#configuration-settings" aria-label="Permalink to &quot;Configuration Settings&quot;">​</a></h1><p>The availability of the above commands depends upon features that may or may not be enabled in the NuttX configuration file. The following <code>cmdtable &lt;nxdiagcmddependencies&gt;</code> indicates the dependency of each command on NuttX configuration settings. General configuration settings are discussed in the NuttX Porting Guide. Configuration settings specific to Nxdiag as discussed at the <code>cmdbottom &lt;nxdiagconfiguration&gt;</code> of this document.</p><p>Note that the <code>--vendor-specific</code> or <code>-v</code> option will generate vendor-specific information and checks. The output of this option will depend on the selected vendors in the NuttX configuration file. For example, if the <code>CONFIG_SYSTEM_NXDIAG_ESPRESSIF</code> configuration setting is enabled, then this option will provide custom information and checks for Espressif devices. Multiple vendors may be selected at the same time.</p><h2 id="option-dependencies-on-configuration-settings" tabindex="-1">Option Dependencies on Configuration Settings <a class="header-anchor" href="#option-dependencies-on-configuration-settings" aria-label="Permalink to &quot;Option Dependencies on Configuration Settings&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Option</th><th>Depends on Configuration</th></tr></thead><tbody><tr><td><code>--help, -h</code> <code>--nuttx, -n</code></td><td></td></tr><tr><td><code>--flags, -f</code></td><td><code>CONFIG_SYSTEM_NXDIAG_COMP_FLAGS</code></td></tr><tr><td><code>--config, -c</code> <code>--host-os, -o</code></td><td><code>CONFIG_SYSTEM_NXDIAG_CONF</code></td></tr><tr><td><code>--host-path, -p</code></td><td><code>CONFIG_SYSTEM_NXDIAG_HOST_PATH</code></td></tr><tr><td><code>--host-packages, -k</code></td><td><code>CONFIG_SYSTEM_NXDIAG_HOST_PACKAGES</code></td></tr><tr><td><code>--host-modules, -m</code> <code>--vendor-specific, -v</code> <code>--all</code></td><td><code>CONFIG_SYSTEM_NXDIAG_HOST_MODULES</code></td></tr></tbody></table><h2 id="nxdiag-specific-configuration-settings" tabindex="-1">Nxdiag-Specific Configuration Settings <a class="header-anchor" href="#nxdiag-specific-configuration-settings" aria-label="Permalink to &quot;Nxdiag-Specific Configuration Settings&quot;">​</a></h2><p>The behavior of Nxdiag can be modified with the following settings in the <code>boards/&lt;arch&gt;/&lt;chip&gt;/&lt;board&gt;/defconfig</code> file:</p><table><thead><tr class="header"><th>Configuration</th><th>Description</th></tr></thead><tbody><tr class="odd"><td><blockquote><p><code>CONFIG_SYSTEM_NXDIAG_COMP_FLAGS</code></p></blockquote></td><td><p>Enable the nxdiag application to list the NuttX compilation flags. This is useful for debugging the host and target systems. Enables the <code>-f</code> and <code>--nuttx-flags</code> options.</p></td></tr><tr class="even"><td><blockquote><p><code>CONFIG_SYSTEM_NXDIAG_CONF</code></p></blockquote></td><td><p>Enable the nxdiag application to list the configuration options used to compile NuttX. This is useful for debugging the host and target systems. Enables the <code>-c</code> and <code>--nuttx-config</code> options.</p></td></tr><tr class="odd"><td><blockquote><p><code>CONFIG_SYSTEM_NXDIAG_HOST_PATH</code></p></blockquote></td><td><p>Enable the nxdiag application to list the host system PATH variable. This is useful for debugging the host system. Enables the <code>-p</code> and <code>--host-path</code> options.</p></td></tr><tr class="even"><td><blockquote><p><code>CONFIG_SYSTEM_NXDIAG_HOST_PACKAGES</code></p></blockquote></td><td><p>Enable the nxdiag application to list the installed packages on the host system. This is useful for debugging the host system. Enables the <code>-k</code> and <code>--host-packages</code> options.</p></td></tr><tr class="odd"><td><blockquote><p><code>CONFIG_SYSTEM_NXDIAG_HOST_MODULES</code></p></blockquote></td><td><p>Enable the nxdiag application to list the installed Python modules on the host system. This is useful for debugging the host system. Enables the <code>-m</code> and <code>--host-modules</code> options.</p></td></tr><tr class="even"><td><blockquote><p><code>CONFIG_SYSTEM_NXDIAG_ESPRESSIF</code></p></blockquote></td><td><p>Enable Espressif-specific information and checks.</p></td></tr></tbody></table>',8)]))}const f=e(n,[["render",a]]);export{g as __pageData,f as default};
