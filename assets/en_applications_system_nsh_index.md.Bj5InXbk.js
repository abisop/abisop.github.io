import{_ as n,c as i,al as o,o as t}from"./chunks/framework.NFAqBSgQ.js";const u=JSON.parse('{"title":"nsh NuttShell (NSH) example","description":"","frontmatter":{},"headers":[],"relativePath":"en/applications/system/nsh/index.md","filePath":"en/applications/system/nsh/index.md"}'),a={name:"en/applications/system/nsh/index.md"};function l(r,e,s,c,d,h){return t(),i("div",null,e[0]||(e[0]=[o(`<h1 id="nsh-nuttshell-nsh-example" tabindex="-1"><code>nsh</code> NuttShell (NSH) example <a class="header-anchor" href="#nsh-nuttshell-nsh-example" aria-label="Permalink to &quot;\`nsh\` NuttShell (NSH) example&quot;">​</a></h1><h2 id="basic-configuration" tabindex="-1">Basic Configuration <a class="header-anchor" href="#basic-configuration" aria-label="Permalink to &quot;Basic Configuration&quot;">​</a></h2><p>This directory provides an example of how to configure and use the NuttShell (NSH) application. NSH is a simple shell application. NSH is described in its own README located at <code>apps/nshlib/README.md</code>. This function is enabled with:</p><pre><code>CONFIG_SYSTEM_NSH=y
</code></pre><p>Applications using this example will need to provide an <code>defconfig</code> file in the configuration directory with instruction to build the NSH library like:</p><pre><code>CONFIG_NSH_LIBRARY=y
</code></pre><h2 id="other-configuration-requirements" tabindex="-1">Other Configuration Requirements <a class="header-anchor" href="#other-configuration-requirements" aria-label="Permalink to &quot;Other Configuration Requirements&quot;">​</a></h2><p><strong>Note</strong>: If the NSH serial console is used, then following is also required to build the <code>readline()</code> library:</p><pre><code>CONFIG_SYSTEM_READLINE=y
</code></pre><p>And if networking is included:</p><pre><code>CONFIG_NETUTILS_NETLIB=y
CONFIG_NETUTILS_DHCPC=y
CONFIG_NETDB_DNSCLIENT=y
CONFIG_NETUTILS_TFTPC=y
CONFIG_NETUTILS_WEBCLIENT=y
</code></pre><p>If the Telnet console is enabled, then the defconfig file should also include:</p><pre><code>CONFIG_NETUTILS_TELNETD=y
</code></pre><p>Also if the Telnet console is enabled, make sure that you have the following set in the NuttX configuration file or else the performance will be very bad (because there will be only one character per TCP transfer):</p><ul><li><code>CONFIG_STDIO_BUFFER_SIZE</code> - Some value <code>&gt;= 64</code></li><li><code>CONFIG_STDIO_LINEBUFFER=y</code></li></ul>`,15)]))}const _=n(a,[["render",l]]);export{u as __pageData,_ as default};
