import{_ as t,c as r,al as o,o as s}from"./chunks/framework.NFAqBSgQ.js";const h=JSON.parse('{"title":"termcurses Terminal Curses control support","description":"","frontmatter":{},"headers":[],"relativePath":"en/applications/system/termcurses/index.md","filePath":"en/applications/system/termcurses/index.md"}'),i={name:"en/applications/system/termcurses/index.md"};function n(a,e,c,d,l,u){return s(),r("div",null,e[0]||(e[0]=[o('<h1 id="termcurses-terminal-curses-control-support" tabindex="-1"><code>termcurses</code> Terminal Curses control support <a class="header-anchor" href="#termcurses-terminal-curses-control-support" aria-label="Permalink to &quot;`termcurses` Terminal Curses control support&quot;">​</a></h1><p>Terminal emulation library for NuttX</p><p>The Termcurses library provides terminal emulation support for performing common screen actions such as cursor movement, foreground / background color control and keyboard escape sequence mapping. The initial release supports only <code>vt100</code> / <code>ansi</code> terminal types, but the library architecture has an extensible interface to allow support for additional emulation types if needed.</p><p>The library can be used standalone or in conjunction with the <code>apps/graphics/pdcurses</code> libraries. The pdcurses libraries have been updated with a _<a href="./.html">termcurses</a> config option which fully integrates the termcurses library automatically.</p><h2 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h2><p>To use the termcurses library, the routines must be initialized by calling the <code>termcurses_initterm()</code> function. This routine accepts a terminal type string identifying the type of terminal emulation support requested. If a <code>NULL</code> pointer is passed, then the routine will check for a <code>TERM</code> environment variable and set the terminal type based on that string. If the emulation type still cannot be determined, the routine will default to <code>vt100</code> emulation type.</p><p>Upon successful initialization, the <code>termcurses_initterm()</code> function will allocate an new terminal context which must be passed with all future termcurses library functions. When this context is no longer needed, the <code>termcurses_deinitterm()</code> routine should be called for proper freeing and terminal teardown.</p><h2 id="use-with-telnetd" tabindex="-1">Use with <code>telnetd</code> <a class="header-anchor" href="#use-with-telnetd" aria-label="Permalink to &quot;Use with `telnetd`&quot;">​</a></h2><p>When using termcurses with the telnet daemon, the telnet config option <code>CONFIG_TELNET_SUPPORT_NAWS</code> should be enabled. This option adds code to the telnet library for terminal size negotiation. Without this option, the telnet routines have no concept of the terminal size, and therefore the termcurses routines must default to <code>80x24</code> screen mode.</p><h2 id="use-with-pdcurses" tabindex="-1">Use with <code>pdcurses</code> <a class="header-anchor" href="#use-with-pdcurses" aria-label="Permalink to &quot;Use with `pdcurses`&quot;">​</a></h2><p>When using the pdcurses termcurses support (i.e you have enabled both the <code>CONFIG_PDCURSES</code> and <code>CONFIG_TERMCURSES</code> options),, the pdcurses input device should be selected to be <code>TERMINPUT</code> (i.e. set <code>CONFIG_PDCURSES_TERMINPUT=y</code>). This causes the pdcurses keyboard input logic to use <code>termcurses_getkeycode()</code> routine for curses input.</p><p>Author: Ken Pettit Date: 2018-2019</p>',12)]))}const m=t(i,[["render",n]]);export{h as __pageData,m as default};
